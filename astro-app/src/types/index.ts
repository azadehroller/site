/**
 * Shared TypeScript types for the astro-app.
 *
 * Currently, most types live alongside their data source:
 *   - Sanity document/block types → src/utils/sanity.ts
 *   - Visual editing types         → src/utils/visualEditing.ts
 *   - API types                    → src/pages/api/*.ts
 *
 * As types grow, extract them here by category:
 *   src/types/sanity.ts    ← Sanity document/block/response shapes
 *   src/types/components.ts ← Shared component prop types
 *   src/types/api.ts       ← API request/response types
 */
