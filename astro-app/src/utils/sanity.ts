// Backward-compatible barrel — all existing imports from "@utils/sanity" continue to work unchanged.
// Source of truth has moved to src/sanity/ — edit files there, not here.
export * from "../sanity/index";
