---
/**
 * ExperimentTracking - logs experiment view data on the client.
 * Replace the console.log with your analytics service (GA, Segment, etc.)
 */
interface Props {
  userGroup: string;
  userId: string;
}

const { userGroup, userId } = Astro.props;
---

<script
  define:vars={{ userGroup, userId }}
>
  // TODO: replace console.log with Google Analytics, Segment, etc.
  console.log('Viewed Experiment, send tracking', {
    userGroup: userGroup,
    userId: userId,
  });
</script>
